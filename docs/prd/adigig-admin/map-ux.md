# AdiGig Map UX — User/Date/Session Flow

- Top controls: user selector (searchable), date presets (Today/7/30), session selector (after user+date)
- Map: downsampled polyline, bbox zoom; summary cards (distance, active/idle, stops/clusters, bbox)
- Performance: fast decode/paint; payload caps; no raw point dumps
- States: clear loading/empty/error; mobile-first (no horiz scroll; safe areas)
